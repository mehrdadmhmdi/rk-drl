# rk-drl
Offline multi-dimensional distributional RL via RKHS embeddings

## Installation
### Works on Linux/macOS/Windows (requires Python ≥3.9, git, and pip). 
```python
# install directly from GitHub
python -m pip install "git+https://github.com/mehrdadmhmdi/rk-drl.git"

```

### Developer install (editable)

```python
# clone the repo
git clone https://github.com/mehrdadmhmdi/rk-drl.git
cd rk-drl

# install in editable mode
python -m pip install -e .
```
